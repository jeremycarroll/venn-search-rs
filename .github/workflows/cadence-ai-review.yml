# Generated by Copier; edit the template source.
name: Cadence AI Review

on:
  workflow_dispatch:
    inputs:
      pr_numbers:
        description: PRs to review, comma- or space-separated
        required: false
        type: string
      review_label:
        description: Review open PRs with this label
        required: false
        type: string

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  review:
    uses: 1000lines/symphony-client-workflows/.github/workflows/cadence-ai-review.yml@ddc9eb0f2a1a24643b6350db9003ed27089548a6
    with:
      helpers-ref: ddc9eb0f2a1a24643b6350db9003ed27089548a6
      pr_numbers: ${{ inputs.pr_numbers }}
      review_label: ${{ inputs.review_label }}
    secrets:
      CADENCE_APP_PRIVATE_KEY: ${{ secrets.CADENCE_APP_PRIVATE_KEY }}
      CADENCE_OPENAI_API_KEY: ${{ secrets.CADENCE_OPENAI_API_KEY }}
      CADENCE_AI_REVIEW_ANTHROPIC_API_KEY: ${{ secrets.CADENCE_AI_REVIEW_ANTHROPIC_API_KEY }}
      CADENCE_LINEAR_API_TOKEN: ${{ secrets.CADENCE_LINEAR_API_TOKEN }}
