# Generated by Copier; edit the template source.
name: Cadence Review Handoff

on:
  workflow_run:
    workflows: [Cadence Review Ingress]
    types: [completed]

permissions:
  contents: read
  pull-requests: read

jobs:
  handoff:
    uses: 1000lines/symphony-client-workflows/.github/workflows/cadence-linear-rework.yml@ddc9eb0f2a1a24643b6350db9003ed27089548a6
    with:
      helpers-ref: ddc9eb0f2a1a24643b6350db9003ed27089548a6
    secrets:
      CADENCE_APP_PRIVATE_KEY: ${{ secrets.CADENCE_APP_PRIVATE_KEY }}
      CADENCE_LINEAR_API_TOKEN: ${{ secrets.CADENCE_LINEAR_API_TOKEN }}
